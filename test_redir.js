const { chromium } = require('playwright'); const path = require('path'); (async () => { const context = await chromium.launchPersistentContext(path.join(__dirname, '.cloudbeds_session'), { headless: true }); const page = await context.newPage(); await page.goto('https://hotels.cloudbeds.com/connect/10074733072513#/reservations/4QW221N1DZ', { waitUntil: 'domcontentloaded' }); console.log('Immediate URL: ' + page.url()); await page.waitForTimeout(3000); console.log('Delayed URL: ' + page.url()); await context.close(); })();