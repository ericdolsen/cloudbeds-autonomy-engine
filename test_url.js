const { chromium } = require('playwright'); const path = require('path'); (async () => { const context = await chromium.launchPersistentContext(path.join(__dirname, '.cloudbeds_session'), { headless: true }); const page = await context.newPage(); await page.goto('https://hotels.cloudbeds.com/connect/', { waitUntil: 'networkidle' }); await page.waitForTimeout(5000); console.log('Current URL is: ' + page.url()); await context.close(); })();