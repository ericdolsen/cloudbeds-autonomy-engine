const { chromium } = require('playwright'); const path = require('path'); (async () => { const context = await chromium.launchPersistentContext(path.join(__dirname, '.cloudbeds_session'), { headless: false }); const page = await context.newPage(); await page.goto('https://hotels.cloudbeds.com/connect/#/reservations/4QW221N1DZ', { waitUntil: 'networkidle' }); await page.waitForTimeout(5000); console.log('Headful URL: ' + page.url()); await context.close(); })();